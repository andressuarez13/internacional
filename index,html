<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Formulario de Registro</title>

<style>
    body {
        font-family: Arial, sans-serif;
        background: linear-gradient(135deg, #F89070, #F0F6DB);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 10px; /* Para evitar que pegue contra los bordes en móvil */
    }

    .container {
        background: linear-gradient(135deg, #F89070, #F0F6DB);
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 4px 10px rgba(0,0,0,0.2);
        width: 100%;
        max-width: 400px;
        box-sizing: border-box;
    }

    h2 {
        text-align: center;
        margin-bottom: 20px;
        color: #fff;
        word-wrap: break-word;
        font-size: 1.4rem;
    }

    label {
        font-weight: bold;
        margin-top: 10px;
        display: block;
        color: #fff;
    }

    input, select {
        width: 100%;
        padding: 10px;
        margin-top: 5px;
        margin-bottom: 15px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 16px;
        box-sizing: border-box;
    }

    button {
        margin-top: 10px;
        width: 100%;
        padding: 12px;
        background: #2DC0FF;
        border: none;
        border-radius: 5px;
        color: white;
        font-size: 18px;
        cursor: pointer;
    }

    button:hover {
        background: #0056b3;
    }

    /* 📱 Ajustes para pantallas pequeñas */
    @media (max-width: 480px) {
        h2 {
            font-size: 1.1rem;
        }
        input, select, button {
            font-size: 14px;
            padding: 10px;
        }
    }
</style>
</head>
<body>
<div class="container">
    <h2>FORMULARIO DE ASISTENCIA INTERNACIONAL</h2>
    <form id="registroForm">


        <label for="NombreCompleto">Nombre completo:</label>
        <input type="text" id="NombreCompleto" name="NombreCompleto" required>

        <label for="Documento">Documento identificación:</label>
        <input type="text" id="Documento" name="Documento" required>

        <label for="Correo">Correo:</label>
        <input type="email" id="Correo" name="Correo" required>

        <label for="fecha de ingreso">fecha de ingreso :</label>
        <input type="date" id="fecha de ingreso" name="fecha de ingreso" required>

        <label for="ambiente">Ambiente de economía circular:</label>
        <select id="ambiente" name="ambiente" required>
            <option value="">-- Selecciona --</option>
            <option value="601">601 - Ideación e innovación</option>
            <option value="602">602 - Diseño y prototipado</option>
            <option value="603">603 - Alistamiento y transformación</option>
        </select>

        <label for="jornada">Selecciona la jornada:</label>
        <select id="jornada" name="jornada" required>
            <option value="">-- Selecciona --</option>
            <option value="mañana">Mañana</option>
            <option value="tarde">Tarde</option>
        </select>

        <label for="Telefono">Número tel:</label>
        <input type="text" id="Telefono" name="Telefono">

        <label for="Rol">Rol:</label>
        <select id="Rol" name="Rol" required>
            <option value="">-- Selecciona --</option>
            <option value="Academica">Organización académica</option>
            <option value="Gubernamental">Organización gubernamental</option>
        </select>

<label for="Pais">País:</label>
<select id="Pais" name="Pais" required>
    <option value="">-- Selecciona --</option>
    <option value="Colombia">Colombia</option>
    <option value="México">México</option>
    <option value="Argentina">Argentina</option>
    <option value="España">España</option>
    <option value="Chile">Chile</option>
    <option value="Perú">Perú</option>
    <option value="Alemania">Alemania</option>
    <option value="Japon">Japon</option>
    <option value="China">China</option>
</select>
        <button type="submit">Guardar</button>
    </form>
</div>

<script>
    document.getElementById("registroForm").addEventListener("submit", async function(e) {
        e.preventDefault();

        // Crear objeto con los datos del formulario
        const data = {
            NombreCompleto: document.getElementById("NombreCompleto").value,
            Documento: document.getElementById("Documento").value,
            Correo: document.getElementById("Correo").value,
            FechaIngreso: document.getElementById("fecha de ingreso").value,
            Ambiente: document.getElementById("ambiente").value,
            Jornada: document.getElementById("jornada").value,
            Telefono: document.getElementById("Telefono").value,
            Rol: document.getElementById("Rol").value,
            Pais: document.getElementById("Pais").value
        };

        try {
            const response = await fetch("https://guardarinformacion-2.onrender.com/guardar", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify(data)
            });

            const result = await response.json();
            if (result.status === "success") {
                alert("Datos guardados correctamente en Excel");
                document.getElementById("registroForm").reset();
            } else {
                alert("Error: " + result.message);
            }
        } catch (error) {
            alert("Error de conexión con el servidor");
            console.error(error);
        }
    });
</script>
</body>
</html>
